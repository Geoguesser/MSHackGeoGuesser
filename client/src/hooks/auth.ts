// import React from "react";
// // import { AuthContext, DefaultAuthContext } from "../contexts/auth";

// function useAuth(): DefaultAuthContext {
//   const context: DefaultAuthContext = React.useContext(AuthContext);
//   if (context === undefined) {
//     throw new Error(`useAuth must be used within an AuthProvider`);
//   }
//   return context;
// }

// export { useAuth };

export function useAuth() {
  return null;
}
